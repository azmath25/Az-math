async function loadList() { const res = await fetch("problems.json"); const problems = await res.json(); const container = document.getElementById("list-container"); problems.forEach(p => { const card = document.createElement("div"); card.className = "problem-card"; card.onclick = () => location.href = `problem${p.id}.html`; const triangle = document.createElement("div"); triangle.className = "triangle"; triangle.style.borderBottomColor = p.color; card.innerHTML = `<h3>Problem ${p.id}</h3><p>${p.statement}</p>`; card.appendChild(triangle); container.appendChild(card); }); }
document.addEventListener("DOMContentLoaded", loadList);
